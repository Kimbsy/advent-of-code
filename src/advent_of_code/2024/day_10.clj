(ns advent-of-code.2024.day-10
  (:require [clojure.data :as d]
            [clojure.java.io :as io]
            [clojure.string :as s]
            [clojure.set :as cset]
            [advent-of-code.util :as u]))

(def input
  (line-seq (io/reader (io/resource "2024/day_10"))))

(def test-input [])

(defn parse-input
  [in]
  in)

(defn part-1
  []
  )

(defn part-2
  []
  )

(comment
  (part-1) ;; => 
  (part-2) ;; => 
  ,)

;; refactoring check
#_(= [(part-1) (part-2)] [])
